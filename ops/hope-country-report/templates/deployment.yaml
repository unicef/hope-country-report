apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hope-country-report.fullname" . }}
  labels:
    {{- include "hope-country-report.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "hope-country-report.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "hope-country-report.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: nginx
          image: "tivix/docker-nginx:v17"
          env:
            - name: UPSTREAMS
              value: "/:localhost:{{ .Values.service.port }}"
            - name: STATICS
              value: "/static:/static"
            - name: NGINX_PORT
              value: "80"
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          volumeMounts:
            - name: statics
              mountPath: /static
              readOnly: true
        - name: backend
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
          - secretRef:
              name: {{ include "hope-country-report.fullname" . }}
          - configMapRef:
              name: {{ include "hope-country-report.fullname" . }}-backend
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          volumeMounts:
          - name: statics
            mountPath: /static
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      initContainers:
      - name: statics-permissions
        image: busybox
        command: ["sh", "-c", "chown -R 82:82 /static"]
        volumeMounts:
        - name: statics
          mountPath: /static
      volumes:
      - name: statics
        persistentVolumeClaim:
          claimName: {{ include "hope-country-report.fullname" . }}-static
