apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hope-country-report.fullname" . }}-test-connection"
  labels:
    {{- include "hope-country-report.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hope-country-report.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
