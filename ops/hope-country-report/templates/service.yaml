apiVersion: v1
kind: Service
metadata:
  name: {{ include "hope-country-report.fullname" . }}
  labels:
    {{- include "hope-country-report.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hope-country-report.selectorLabels" . | nindent 4 }}
